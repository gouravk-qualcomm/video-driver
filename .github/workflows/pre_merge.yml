name: pre_merge
description: |
  Orchestrates pre-merge CI for the video-driver repository a matrix
  in the caller workflow. Builds and tests using reusable workflows.

on:
  push:
    branches:
      - video.qclinuxmain.root
  workflow_dispatch:
  pull_request_target:
    types: [opened, synchronize, reopened]
    branches:
      - video.qclinuxmain.root

jobs:
  loading:
    uses: qualcomm-linux/video-driver/.github/workflows/loading.yml@video.qclinuxmain.root
    secrets: inherit

  build:
    needs: loading
    uses: qualcomm-linux/video-driver/.github/workflows/sync-and-build.yml@video.qclinuxmain.root
    secrets: inherit
    with:
      docker_image: kmake-image:ver.1.0
      build_matrix: ${{ needs.loading.outputs.build_matrix }}

  lava-test:
    needs: [loading, build]
    uses: qualcomm-linux/video-driver/.github/workflows/test.yml@video.qclinuxmain.root
    secrets: inherit
    with:
      docker_image: ghcr.io/qualcomm-linux/kmake-image:ver.1.0
      build_matrix: ${{ needs.loading.outputs.build_matrix }}
      full_matrix: ${{ needs.loading.outputs.full_matrix }}