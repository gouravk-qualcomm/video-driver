---
name: pre_merge
run-name: Pre Merge Workflow
on:
  workflow_dispatch:
  pull_request: # requiring access to base repo
    types: [opened, synchronize, reopened]
    branches:
      - fix-mounting-3

jobs:
  loading:
    uses: qualcomm-linux/video-driver/.github/workflows/loading.yml@fix-mounting-3
    secrets: inherit

  build:
    needs: loading
    uses: qualcomm-linux/video-driver/.github/workflows/build.yml@fix-mounting-3
    secrets: inherit
    with:
      build_matrix: ${{ needs.loading.outputs.build_matrix }}
  
  process_image:
    needs: [loading, build]
    uses: qualcomm-linux/video-driver/.github/workflows/process_image.yml@fix-mounting-3
    with:
      build_matrix: ${{ needs.loading.outputs.build_matrix }}
