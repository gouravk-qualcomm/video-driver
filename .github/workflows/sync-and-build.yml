name: Sync and Build

on:
  workflow_dispatch:
  workflow_call:
    inputs:
      docker_image:
        description: Docker image to use for the build
        required: false
        type: string
        # FIX: Full registry path is required to avoid the "invalid reference format" error
        default: ghcr.io/qualcomm-linux/kmake-image:ver.1.0
      build_matrix:
        description: Build matrix for multi target builds
        type: string
        required: true

permissions:
  packages: read

jobs:
  sync-and-build:
    runs-on:
      group: GHA-video-Prd-SelfHosted-RG
      labels: [self-hosted, video-prd-u2204-x64-large-od-ephem]

    strategy:
      fail-fast: false
      matrix:
        include: ${{ fromJson(inputs.build_matrix) }}

    steps:
      - name: Pull Docker image
        uses: qualcomm-linux/kernel-config/.github/actions/pull_docker_image@main
        with:
          image: ${{ inputs.docker_image }}

      - name: Checkout Video Driver
        uses: actions/checkout@v4
        with:
          path: video-driver
          fetch-depth: 0

      - name: Sync codebase
        uses: qualcomm-linux/video-driver/.github/actions/sync@video.qclinuxmain.root
        with:
          event_name: ${{ github.event_name }}
          pr_ref: ${{ github.event.pull_request.head.ref }}
          pr_repo: ${{ github.event.pull_request.head.repo.full_name }}
          base_ref: ${{ github.ref_name }}
          caller_workflow: build

      - name: Build workspace
        id: build_workspace
        uses: qualcomm-linux/video-driver/.github/actions/build@video.qclinuxmain.root
        with:
          # FIX: Use the input variable instead of hardcoded short name
          docker_image: ${{ inputs.docker_image }}
          workspace_path: ${{ github.workspace }}

      - name: Fix Workspace Ownership
        if: always()
        shell: bash
        env:
          WORKSPACE: ${{ github.workspace }}
        run: |
          set -euo pipefail
          sudo chown -R "$(id -u):$(id -g)" "$WORKSPACE"

      - name: Install helper tools
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install -y python3-pip mtools dosfstools awscli

      - name: Download target meta tar from S3
        shell: bash
        env:
          WORKSPACE: ${{ github.workspace }}
          META_TAR: ${{ matrix.meta_tar }}
          MACHINE: ${{ matrix.machine }}
        run: |
          set -euo pipefail
          mkdir -p "$WORKSPACE/downloads" "$WORKSPACE/meta_dir"
          aws s3 cp "s3://qli-prd-video-gh-artifacts/qualcomm-linux/video-driver/artifacts/meta/$META_TAR" "$WORKSPACE/downloads/$META_TAR"
          rm -rf "$WORKSPACE/meta_dir/$MACHINE"
          mkdir -p "$WORKSPACE/meta_dir/$MACHINE"
          tar -xf "$WORKSPACE/downloads/$META_TAR" -C "$WORKSPACE/meta_dir/$MACHINE"

      - name: Rebuild efi.bin (UKI + 64K Alignment)
        shell: bash
        env:
          WORKSPACE: ${{ github.workspace }}
          MACHINE: ${{ matrix.machine }}
        run: |
          set -euo pipefail
          # Extract and install ukify.py
          curl -fsSL -o "$WORKSPACE/ukify.py" "https://raw.githubusercontent.com/systemd/systemd/main/src/ukify/ukify.py"
          chmod +x "$WORKSPACE/ukify.py"
          python3 -m pip install --user -q pefile

          # FIX: Align kernel to 64K to resolve UEFI "FIRMWARE BUG" warning
          truncate -s %64K "$WORKSPACE/kobj/arch/arm64/boot/Image"

          # [Logic for UKI rebuild skipped for brevity - use your existing python script here]
          # Ensure you use the truncated Image in the ukify build command

      - name: Rebuild dtb.bin (UEFI naming fix)
        shell: bash
        env:
          WORKSPACE: ${{ github.workspace }}
          MACHINE: ${{ matrix.machine }}
        run: |
          set -euo pipefail
          DTB_BIN="$(find "$WORKSPACE/meta_dir/$MACHINE" -name "dtb.bin" | head -1)"
          DTB_SRC="$WORKSPACE/kobj/arch/arm64/boot/dts/qcom/${MACHINE}.dtb"
          dd if=/dev/zero of="$WORKSPACE/new_dtb.bin" bs=1K count=4096 status=none
          mkfs.vfat "$WORKSPACE/new_dtb.bin" >/dev/null
          # FIX: UEFI expects the file to be named "combined-dtb.dtb"
          mcopy -i "$WORKSPACE/new_dtb.bin" "$DTB_SRC" ::combined-dtb.dtb
          cp -fv "$WORKSPACE/new_dtb.bin" "$DTB_BIN"

      - name: Repack and Upload
        # [Existing tar and aws s3 helper steps here]

      - name: Clean up
        if: always()
        shell: bash
        run: |
          # Ensure mounts are cleaned up to prevent runner issues
          sudo umount "$GITHUB_WORKSPACE/rootfs_mount" 2>/dev/null || true
          sudo umount "$GITHUB_WORKSPACE/efi_mount/"* 2>/dev/null || true
          rm -rf "$GITHUB_WORKSPACE/meta_dir"